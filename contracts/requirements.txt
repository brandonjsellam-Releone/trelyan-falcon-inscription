# TRELYAN inscription — test / deploy virtualenv (the RUNTIME side).
# Python 3.12+ works here; pytest drives localnet (test_inscription.py) and deploy_testnet.py runs TestNet.
algokit-utils>=4,<5
pytest>=8
py-algorand-sdk>=2

# --- Falcon (off-chain deterministic signer) ---
# Not a pip package: build the algorand/falcon C library into libfalcondet1024.so and set
# FALCON_DET1024_LIB to its path (see falcon_det1024.py header for the exact `cc` line).

# --- COMPILATION is a SEPARATE, pinned environment (PuyaPy needs Python <=3.13, NOT 3.14) ---
# The pins now live in an INSTALLABLE file rather than in this comment. A commented pin cannot be
# installed, so CI could not reproduce the build even in principle and nothing detected the
# toolchain moving:
#   pip install -r contracts/requirements-compile.txt      (puyapy==5.8.1, algorand-python==3.5.0)
#
# Build: run FROM contracts/ with the BARE filename. The invocation is part of the artifact --
# puya writes the source path as typed into the TEAL comments, and relative to the out-dir in the
# .puya.map, so compiling from anywhere else yields 124 cosmetically-different lines. The paths
# below previously read crypto/contracts/..., a directory that does not exist in this repository.
#   (cd contracts && puyapy inscription.py --out-dir out --target-avm-version 12)
# Then:
#   algokit generate client contracts/out/TrelyanInscription.arc56.json --output contracts/trelyan_client.py
# Enforced in CI by contracts/verify_teal_matches_source.py.
