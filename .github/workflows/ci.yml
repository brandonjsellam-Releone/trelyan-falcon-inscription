name: trelyan-pq CI

on:
  # contracts/** must stay in these filters. It was absent until 2026-08-11, so no contract
  # change ever triggered CI: inscription.py could be edited and merged without a single test
  # running, and with nothing to notice it no longer matched the deployed application. That is
  # precisely what happened in 2ec798e (2026-06-16), and it went unseen for two months.
  # third_party/** was added 2026-08-18 with the vendored Falcon tree: every job that builds the
  # library now builds it from there, so a change to those bytes must run the KAT and the
  # sanitizer gate exactly like a change to the SDK would.
  push:
    paths: ["sdk/**", "contracts/**", "third_party/**", ".github/workflows/ci.yml"]
  pull_request:
    paths: ["sdk/**", "contracts/**", "third_party/**", ".github/workflows/ci.yml"]
  schedule:
    - cron: "23 6 * * 1"   # Mondays 06:23 UTC — continuous proof the deployed app still verifies
  workflow_dispatch: {}

jobs:
  secret-scan:
    name: Secret scan (gitleaks)
    # .gitleaks.toml has existed since the repo was published but nothing ever ran it - no CI job,
    # no pre-commit hook - so its rules (including the deliberate allow-list for the PUBLIC TEST
    # VECTOR key in sdk/tests/vectors/) were configuration without enforcement. The repo's
    # deployment path uses DEPLOYER_MNEMONIC, an Algorand mnemonic that controls its account
    # outright, so an accidentally committed .env is an irrevocable key disclosure in git history.
    #
    # WHICH COMMITS ACTUALLY GET SCANNED, because it is not the same on every trigger and the
    # difference is the whole value of this job. From gitleaks-action v2 at the pinned SHA,
    # src/gitleaks.js:103-115: `push` and `pull_request` are given
    #   --log-opts=--no-merges --first-parent <base>^..<head>
    # i.e. ONLY the commits in that push or PR. Every other supported event (src/index.js:176 -
    # `schedule`, `workflow_dispatch`) is run with no --log-opts at all, which is what makes
    # gitleaks walk the entire history.
    #
    # This job previously carried `if: github.event_name != 'schedule'`, which excluded the one
    # automatic trigger that scans everything. Verified on scheduled run 31368848691: the job
    # does not appear at all. So push and PR each scanned a handful of commits, the weekly run
    # scanned nothing, and full history was never covered on any automatic path - while the
    # comment here claimed it was. A secret committed before the range being pushed was not
    # caught by anything.
    #
    # Removing the condition is the fix: the Monday schedule now performs the full-history scan
    # that makes the claim above true. fetch-depth: 0 below is what makes that possible, and was
    # already correct - it was the trigger that was missing, not the checkout.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.3.1
        with:
          fetch-depth: 0   # gitleaks needs full history to scan past commits
      - uses: gitleaks/gitleaks-action@ff98106e4c7b2bc287b24eaf42907196329070c7 # v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_CONFIG: .gitleaks.toml

  build-recipe-consistency:
    name: Every published build recipe is pinned and hardened
    # The repo had FIVE user-facing recipes cloning the algorand/falcon DEFAULT BRANCH (no commit,
    # no digest) and compiling with `cc -O3 -fPIC -shared`, omitting -DFALCON_UNALIGNED=0 and
    # -fno-strict-aliasing - while EVERY automated path (this workflow, .env.example,
    # Dockerfile.repro, scripts/verify_all.sh) carried commit ce15e75 plus both flags. The split
    # was total: no user-facing recipe referenced PINNED_BUILD.md, and one sat under a heading
    # reading "Reproduction (pinned)" while pinning nothing.
    #
    # Not cosmetic. This workflow's OWN sanitizer gate asserts the DEFAULT autodetected build MUST
    # trap under -fsanitize=alignment (it fails with "gate vacuous" if it does not), so the
    # published recipe was demonstrably UB-positive. `pip install trelyan-pq` makes sdk/README.md
    # the PyPI long description, so this was the recipe users actually followed to build the signer
    # that authorizes on-chain writes.
    #
    # Escalation to key recovery is NOT claimed: PINNED_BUILD.md records the pin and upstream main
    # as byte-identical over 120 signatures plus the KAT, so on tested toolchains the flags change
    # no output. The harm is latent UB plus loss of pin provenance.
    #
    # A docs-consistency grep is a weaker control than a test, and is labelled as one - but the
    # thing being protected IS a set of documents, so a grep over the tree is the right shape.
    runs-on: ubuntu-latest
    if: github.event_name != 'schedule'
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.3.1

      - name: No recipe may clone the default branch
        # This file is EXCLUDED from the search, and that exclusion is load-bearing rather than
        # lazy: the search strings appear literally in this job, so a tree-wide grep would match
        # itself and the gate would fail permanently on its own text. Automated Falcon builds in
        # this workflow already fetch the pinned tarball and are covered by the digest gate in
        # signature-kat, so nothing is left unguarded by the exclusion.
        run: |
          if git grep -n "clone https://github.com/algorand/falcon" -- . ':!.github/workflows/ci.yml' ; then
            echo "::error::a build recipe clones the DEFAULT BRANCH. Use the pinned tarball -"
            echo "::error::  https://github.com/algorand/falcon/archive/<PIN>.tar.gz  (PINNED_BUILD.md)"
            exit 1
          fi
          echo "no default-branch clones outside this workflow"

      - name: Every cc recipe carries both hardening flags
        run: |
          # `cc -O3` is the anchor because the recipes are multi-line: the source list and even
          # `-o libfalcondet1024.so` can sit on a continuation line, so anchoring on either misses
          # most of them. Verified against the tree: this matches all 7 recipe sites.
          bad=$(git grep -n "cc -O3" -- . ':!.github/workflows/ci.yml' | grep -v "FALCON_UNALIGNED=0" || true)
          if [ -n "$bad" ]; then
            echo "::error::build recipe(s) missing -DFALCON_UNALIGNED=0 / -fno-strict-aliasing:"
            echo "$bad"
            exit 1
          fi
          n=$(git grep -l "cc -O3" -- . ':!.github/workflows/ci.yml' | wc -l | tr -d ' ')
          echo "recipe sites found: $n (all hardened)"
          # NON-VACUITY. If the pattern ever stops matching - a rename, a reformat - this gate
          # would pass by finding nothing at all, which is the exact defect class this repository's
          # register is about. The floor is deliberately below today's 7 so ordinary edits do not
          # trip it, while a collapse to zero or near-zero does.
          if [ "$n" -lt 4 ]; then
            echo "::error::only $n recipe site(s) matched (expected 7). This gate has probably gone"
            echo "::error::vacuous - the pattern no longer finds the recipes. Repair the pattern;"
            echo "::error::do not lower this floor."
            exit 1
          fi
  vendored-falcon-integrity:
    name: Vendored Falcon tree is byte-exact to the pin (offline)
    # third_party/falcon-det1024/src is the pinned algorand/falcon@ce15e75b tree, vendored
    # 2026-08-18 so no job depends on GitHub serving that tarball at build time. Two checks, on
    # purpose: SHA256SUMS is the per-file manifest a reviewer can read line by line; the digest
    # script is the SAME check CI applied to the downloaded tarball before the tree was vendored,
    # so "vendored" and "pinned" remain one claim, not two. Either failing blocks the KAT jobs
    # below from building a drifted signer.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.3.1
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.12"
      - name: Per-file manifest (27 files under src/)
        working-directory: third_party/falcon-det1024
        run: |
          set -o pipefail
          sha256sum -c SHA256SUMS
          # The manifest must be COMPLETE: a file added to src/ without a SHA256SUMS line would
          # otherwise be silently unverified.
          test "$(find src -type f | wc -l)" = "$(wc -l < SHA256SUMS)"
      - name: Pinned tree digest + emulated FP backend (same tool as before vendoring)
        run: python sdk/ci/verify_pinned_digest.py third_party/falcon-det1024/src

  wire-format:
    name: Wire-format tests (no network, no native lib)
    runs-on: ubuntu-latest
    if: github.event_name != 'schedule'
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.3.1
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install (dev extra) and run the wire-format + golden-vector suite
        working-directory: sdk
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
          pytest tests -v

  verify-live:
    name: Live TestNet verification (read-only, no secrets)
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request'
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.3.1
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.13"
      - name: Install the published package from PyPI and verify the deployed app
        run: |
          python -m pip install --upgrade pip
          pip install trelyan-pq
          python sdk/examples/verify_trelyan.py
        # 14 checks: package constants, pinned golden vectors, live app 763809096
        # (bytecode fingerprint), on-chain Falcon key + inscription record, and a
        # byte-exact local rebuild of the domain-separated message. Exits non-zero
        # on any failure. No account, no mnemonic, no secrets.

  teal-matches-source:
    name: Committed TEAL is what the source compiles to
    # Closes the FIRST link of the derivation chain, which nothing checked.
    #
    # contract-drift (below) starts at the committed artifact and works down to the chain.
    # Nothing worked UPWARD from inscription.py to the artifact: verify_deployment.py has a
    # --recompile flag for exactly that, and no workflow ever passed it. So the one
    # human-readable file in the chain - the source a reviewer reads and an auditor is handed -
    # was not verified to correspond to anything. A behaviour-changing edit committed without
    # regenerating contracts/out/ passed every gate green.
    #
    # This job needs the PINNED compiler (contracts/requirements-compile.txt): comparing bytes
    # against a committed artifact is only meaningful against a fixed puya version, since a
    # different version may emit different but equally valid TEAL.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.3.1
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.12"   # puya tracks <=3.13; 3.12 matches the documented build venv
      - name: Install the pinned compile toolchain
        run: python -m pip install -r contracts/requirements-compile.txt
      - name: Recompile inscription.py and compare against the committed artifacts
        run: python contracts/verify_teal_matches_source.py
        # Exit 1 = drift, and the output distinguishes two kinds that need different responses:
        #          BEHAVIOURAL (instruction streams differ - the artifact does not implement the
        #          source) from STALE (identical instructions, stale '// inscription.py:NNN'
        #          references - bytecode unaffected, traceability broken).
        # Exit 2 = could not check (toolchain or artifact missing). Never reported as agreement.

  contract-drift:
    name: Deployed app matches the committed contract (non-circular)
    # verify_trelyan.py pins EXPECTED_APPROVAL_SHA512_256, a constant copied from the DEPLOYED
    # program, and compares it to the chain. Because the contract blocks Update and Delete
    # (invariants I1/I5) the deployed bytecode is immutable, so that comparison cannot fail -
    # including when contracts/ diverges from the deployment, which it has since 2ec798e. The
    # weekly schedule below has therefore been re-proving a tautology, not the deployment.
    #
    # This job derives the expected bytecode from the committed artifact instead, so it CAN
    # fail. It is expected to fail until the committed contract is redeployed; that red is the
    # finding, not a broken job. Do not add continue-on-error here - a gate that cannot fail is
    # the exact defect being corrected.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.3.1
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.13"
      - name: Assemble the committed TEAL and compare it to the deployed program
        run: python contracts/verify_deployment.py
        # Exit 1 = drift: the deployment is not what the committed source builds.
        # Exit 2 = could not check (network, missing artifact). Distinct on purpose: an
        # unreachable node must never be reported as agreement, nor as drift.

  reviewer-containers:
    name: The reviewer-facing containers actually run
    # NOTHING in CI ran either Dockerfile, or scripts/verify_all.sh, or any Makefile target:
    # `grep -rniE docker .github/` returned nothing. So every entry point REVIEWER.md and
    # AUDIT_READINESS.md hand to an external auditor was unexercised, and two of them were broken:
    #
    #   * Dockerfile.verify copied the script bare into WORKDIR /trelyan, so its `parents[2]`
    #     resolution of the committed TEAL raised IndexError at MODULE LEVEL - before the first
    #     print. The documented command produced a traceback and ran zero checks. Failed closed.
    #   * Dockerfile.repro omitted contracts/ entirely, so the source-correspondence check
    #     silently did not run. Because a skipped check counted as neither pass nor fail, the PASS
    #     COUNT WAS IDENTICAL either way: 17 passed / 1 failed from a clone, 17 passed / 0 failed
    #     from the container. It returned clean green on the exact divergence contract-drift is
    #     red on, and nothing signalled the dropped check.
    #
    # WHAT THIS JOB ASSERTS, and deliberately what it does NOT.
    #
    # It asserts the containers BUILD, RUN, and report "0 not checked" - i.e. every check they
    # claim to perform actually executed. It does NOT assert a particular verdict: whether the
    # deployment matches the committed contract is contract-drift's job, and encoding today's
    # known drift here would make this job wrong the moment the redeploy lands.
    #
    # So this job stays valid across the redeploy, and it CAN fail: it goes red if a container
    # stops building, if the script crashes again, or if any check silently stops running.
    runs-on: ubuntu-latest
    if: github.event_name != 'schedule'
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.3.1

      - name: Build both reviewer images
        run: |
          docker build -f Dockerfile.verify -t trelyan-verify .
          docker build -f Dockerfile.repro  -t trelyan-repro  .

      - name: Dockerfile.verify runs every check it claims (exit 0/1/2, never a traceback)
        run: |
          set +e
          docker run --rm trelyan-verify > verify.log 2>&1
          rc=$?
          set -e
          echo "exit=$rc"; cat verify.log

          # A traceback means the script died before reporting - the TCE-71 regression.
          if grep -q "Traceback (most recent call last)" verify.log; then
            echo "::error::verify_trelyan.py crashed inside the container instead of reporting checks"
            exit 1
          fi
          # No RESULT line means it never got to the end.
          if ! grep -qE "^== RESULT: " verify.log; then
            echo "::error::no RESULT line - the script did not complete"
            exit 1
          fi
          # "not checked" > 0 means a check the docs advertise silently did not run. This is the
          # assertion that would have caught TCE-70, and it does not depend on the drift verdict.
          if ! grep -qE "^== RESULT: .* 0 not checked ==" verify.log; then
            echo "::error::a check did not run - see the SKIP lines above. A skipped check is not a pass."
            grep -E "^  SKIP  " verify.log || true
            exit 1
          fi
          # rc 2 is reserved for "could not check", which the line above has already ruled out.
          if [ "$rc" = "2" ]; then
            echo "::error::exit 2 (could not check) with 0 not-checked reported - inconsistent"
            exit 1
          fi
          echo "verify container ran every check it claims (exit $rc; 1 = a check FAILED, which is contract-drift's finding, not this job's)"

      - name: Dockerfile.repro carries contracts/ so source correspondence can be checked
        run: |
          # Direct assertion on the image contents, independent of the script's own reporting.
          # The COPY was missing for long enough to ship a falsely-green hermetic verifier;
          # asserting the file is present is cheap and cannot be satisfied by accident.
          docker run --rm trelyan-repro test -f contracts/out/TrelyanInscription.approval.teal
          echo "committed TEAL is present in the repro image"

      - name: scripts/verify_all.sh is syntactically sound and its KAT axis cannot silently skip
        run: |
          # The full run needs the pinned Falcon build and network, which signature-kat and
          # verify-live already cover. What was never checked is that this script PARSES - it is
          # handed to reviewers and had never been executed by CI in any form.
          sh -n scripts/verify_all.sh
          # And that Axis C arms the library, not just the fixture. Grepping the source is weaker
          # than running it, and is honest about being a tripwire: the real assertion lives in
          # sdk/tests/test_signature_kat.py::test_the_library_is_available_when_required.
          grep -q "TRELYAN_REQUIRE_KAT=1" scripts/verify_all.sh
          echo "verify_all.sh parses and arms the KAT gate"

  signature-kat:
    name: Signature byte-identity KAT (pinned Falcon build, 3 OSes)
    # Build-divergence control (reviewer Q11/Q7): the SAME committed golden signatures must be
    # reproduced byte-for-byte on Linux, macOS, and Windows. Skips cleanly until the fixture
    # (tests/vectors/det1024_kat.json) is generated and committed (see gen_det1024_kat.py).
    if: github.event_name != 'schedule'
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.3.1
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.12"

      - name: Stage the vendored pinned Falcon tree (commit ce15e75, no network)
        shell: bash
        # Until 2026-08-18 this step downloaded the GitHub source tarball on every run (chosen
        # over `git clone` because a clone applies the runner's core.autocrlf and changed the C
        # bytes). The tree is now vendored at third_party/falcon-det1024/src, byte-identical to
        # that tarball (see PROVENANCE.md), so the build no longer depends on GitHub serving an
        # archive, and the `vendored-falcon-integrity` job checks the bytes on every push. The
        # copy keeps the rest of this job's paths unchanged. `.gitattributes` must never mark
        # these files for CRLF conversion — the digest gate below would catch it if it did.
        run: cp -r third_party/falcon-det1024/src falcon-src

      - name: Assert pinned-build digest + emulated FP backend (fail fast if the tree drifted)
        shell: bash
        # Verifies tree digest c6adf48…, deterministic.c 601390dc…, AND that config.h pins
        # FALCON_FPEMU=1 / FALCON_FPNATIVE=0 — the precondition that makes cross-platform
        # byte-identity sound (bit-exact integer FP). Drift here fails the job.
        run: python sdk/ci/verify_pinned_digest.py falcon-src

      - name: Record toolchain (for the fixture/run audit trail)
        shell: bash
        run: |
          echo "OS: ${{ runner.os }} / $(uname -a 2>/dev/null || echo windows)"
          (cc --version 2>/dev/null | head -1) || true
          python --version

      # Sanitizer GATE (Linux): proves the shipped portable build (-DFALCON_UNALIGNED=0 -fno-strict-aliasing)
      # is free of the misaligned-uint64 load at inner.h:846 AND of any other runtime-UB class, across a
      # BROAD surface (keygen+sign+verify+reject over 2 keypairs x {0,9,100,4096}-byte messages — not a
      # single happy path), AND that the alignment gate is non-vacuous (the DEFAULT autodetected build
      # still traps on x86_64). See FALCON_BUILD_HARDENING §2. printf-built driver avoids YAML-heredoc pitfalls.
      - name: Sanitizer gate (portable build is alignment- and UBSan-clean; default build traps)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          cd falcon-src
          printf '%s\n' \
            '#include <stdio.h>' '#include <string.h>' '#include "deterministic.h"' \
            'static int one(unsigned sb,const unsigned char*msg,size_t ml){' \
            ' shake256_context r; unsigned char sd[32]; memset(sd,(int)sb,sizeof sd);' \
            ' shake256_init(&r); shake256_inject(&r,sd,sizeof sd); shake256_flip(&r);' \
            ' unsigned char sk[FALCON_DET1024_PRIVKEY_SIZE], pk[FALCON_DET1024_PUBKEY_SIZE];' \
            ' if(falcon_det1024_keygen(&r,sk,pk)) return 11;' \
            ' unsigned char s[FALCON_DET1024_SIG_COMPRESSED_MAXSIZE]; size_t n=sizeof s;' \
            ' if(falcon_det1024_sign_compressed(s,&n,sk,msg,ml)) return 12;' \
            ' if(falcon_det1024_verify_compressed(s,n,pk,msg,ml)) return 13;' \
            ' s[n/2]^=0xFF; if(falcon_det1024_verify_compressed(s,n,pk,msg,ml)==0) return 14;' \
            ' return 0; }' \
            'int main(void){ static unsigned char b[4096]; size_t i;' \
            ' for(i=0;i<sizeof b;i++) b[i]=(unsigned char)(i*131u+7u);' \
            ' size_t L[]={0,9,100,4096}; int t=0; unsigned k; size_t j;' \
            ' for(k=0;k<2;k++) for(j=0;j<4;j++){ int rc=one(0x10+k,b,L[j]);' \
            '   if(rc){ fprintf(stderr,"FAIL k=%u len=%zu rc=%d\n",k,L[j],rc); return rc; } t++; }' \
            ' printf("OK %d cases keygen+sign+verify+reject\n",t); return 0; }' > _san_probe.c
          SRC="codec.c common.c falcon.c fft.c fpr.c keygen.c rng.c shake.c sign.c vrfy.c deterministic.c _san_probe.c"
          # (1) portable build: ZERO misaligned-load diagnostics across the full surface
          cc -DFALCON_UNALIGNED=0 -O0 -g -fsanitize=alignment -fno-sanitize-trap=alignment -o san_align $SRC
          OUT0=$(./san_align 2>&1); echo "alignment/portable: $OUT0"
          echo "$OUT0" | grep -qi "misaligned" && { echo "FAIL: portable build STILL misaligned"; exit 1; }
          echo "$OUT0" | grep -q "OK .* cases" || { echo "FAIL: portable build did not complete the surface"; exit 1; }
          # (2) portable build: ZERO runtime-UB of ANY class under full UBSan + -fno-strict-aliasing
          cc -DFALCON_UNALIGNED=0 -fno-strict-aliasing -O0 -g -fsanitize=undefined -fno-sanitize-trap=undefined -o san_ub $SRC
          OUT2=$(./san_ub 2>&1); echo "ubsan/portable: $(echo "$OUT2" | tail -1)"
          N=$(echo "$OUT2" | grep -ic "runtime error" || true); echo "UBSan runtime-error lines: $N"   # || true: grep exits 1 on 0 matches (the clean case) and would abort set -e
          [ "$N" = "0" ] || { echo "FAIL: residual UB under -fsanitize=undefined"; echo "$OUT2" | grep -i "runtime error" | head; exit 1; }
          # (3) gate must be REAL: the DEFAULT autodetected build MUST trap on x86_64 (else the test proves nothing)
          if [ "$(uname -m)" = "x86_64" ]; then
            cc -O0 -g -fsanitize=alignment -fno-sanitize-trap=alignment -o san_default $SRC
            OUT1=$(./san_default 2>&1 || true); echo "default: $(echo "$OUT1" | grep -i misaligned | head -1)"
            echo "$OUT1" | grep -qi "misaligned" || { echo "FAIL: gate vacuous — default build did NOT trap"; exit 1; }
            echo "default build traps as expected -> gate is non-vacuous"
          fi
          echo "SANITIZER GATE PASSED (alignment-clean + UBSan-clean over the full sign/verify surface)"

      # --- build the deterministic Falcon-1024 shared library, per OS ---
      # The pinned config.h already pins the emulated FP backend (FALCON_FPEMU=1); do NOT add
      # -ffast-math or -DFALCON_FPNATIVE — either would break the byte-identity the KAT asserts.
      # -DFALCON_UNALIGNED=0 selects upstream's portable byte-wise PRNG read instead of the misaligned
      # uint64 fast path (inner.h:846) — eliminates the alignment UB; -fno-strict-aliasing makes the
      # remaining FALCON_LE type-punning casts (e.g. rng.c) safe against TBAA mis-compilation. Both are
      # PROVEN byte-identical to the goldens (see FALCON_BUILD_HARDENING §2) — they are -D/-f flags, not
      # source edits, so the pinned tree digest is unchanged. The sanitizer gate above enforces this build
      # stays alignment- and UBSan-clean. (MSVC omits -fno-strict-aliasing: cl does not assume strict TBAA.)
      - name: Build shared library (Linux/macOS)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          cd falcon-src
          EXT=so; [ "$(uname)" = "Darwin" ] && EXT=dylib
          cc -O3 -fPIC -DFALCON_UNALIGNED=0 -fno-strict-aliasing -shared -o "libfalcondet1024.$EXT" \
            codec.c common.c falcon.c fft.c fpr.c keygen.c rng.c shake.c sign.c vrfy.c deterministic.c
          echo "FALCON_DET1024_LIB=$PWD/libfalcondet1024.$EXT" >> "$GITHUB_ENV"

      - name: Set up MSVC (Windows)
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@0b201ec74fa43914dc39ae48a89fd1d8cb592756 # v1.13.0

      - name: Build shared library (Windows / MSVC)
        if: runner.os == 'Windows'
        shell: cmd
        # /fp:precise is MSVC's default (NOT /fp:fast) — keep it so FP semantics match.
        # /EXPORT is load-bearing, not tidiness. MSVC exports NOTHING from a DLL without
        # __declspec(dllexport) or explicit /EXPORT, whereas ELF and Mach-O export every
        # non-static symbol. The upstream Falcon sources carry no dllexport annotations, and
        # S2.6 forbids editing the audited reference to add them - so the exports are declared
        # here, at link time, instead.
        #
        # Without this the DLL built, linked, and loaded, and had an EMPTY export table: every
        # ctypes lookup raised, _lib_available() caught it, and all thirteen lib-gated tests
        # "skipped" with the reason "FALCON_DET1024_LIB not built/set" - which was false, since
        # the variable was set correctly to a real file. The Windows leg of this 3-OS
        # byte-identity matrix therefore verified NOTHING from the day it was written, while
        # reporting green, and the cross-platform reproducibility claim it exists to support
        # (reviewer Q11/Q7) held only on Linux and macOS.
        #
        # The four names are exactly the SDK's audited binding surface, so the Windows export
        # table is the allowlist: adding a binding without exporting it fails loudly here.
        run: |
          cd falcon-src
          cl /O2 /LD /DFALCON_UNALIGNED=0 /Fe:falcondet1024.dll ^
            codec.c common.c falcon.c fft.c fpr.c keygen.c rng.c shake.c sign.c vrfy.c deterministic.c ^
            /link ^
            /EXPORT:shake256_init_prng_from_system ^
            /EXPORT:falcon_det1024_keygen ^
            /EXPORT:falcon_det1024_sign_compressed ^
            /EXPORT:falcon_det1024_verify_compressed
          echo FALCON_DET1024_LIB=%CD%\falcondet1024.dll>> %GITHUB_ENV%

      - name: Install SDK (dev extra)
        working-directory: sdk
        shell: bash
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run the signature KAT (byte-identity vs the committed golden)
        working-directory: sdk
        shell: bash
        # TRELYAN_REQUIRE_KAT=1 makes the job FAIL (not skip) if the fixture is still the sentinel,
        # so a "passing" 3-OS run can never silently mean the KAT was skipped.
        env:
          TRELYAN_REQUIRE_KAT: "1"
        run: pytest tests/test_signature_kat.py -v

      - name: Run the lib-gated security tests, and prove they were not skipped
        working-directory: sdk
        shell: bash
        # Until 2026-08-12 these had NEVER executed in CI, on any event. `wire-format` runs the
        # whole suite but does not build the library, so everything behind
        # `skipif(not _lib_available())` skipped there; this job builds the library but ran only
        # test_signature_kat.py. The intersection was empty, and the never-run set included the
        # tests enforcing the constitution's two hardest crypto rules:
        #   test_seal_wipes_private_key_buffer          - S2.3 zeroization
        #   test_worker_stdout_carries_only_public_values - S2.3 no secrets in output
        #   test_isolated_result_has_no_private_key
        #   test_bad_build_fails_closed_without_consuming_cell
        # plus isolated-signer containment and the signature fuzzers: 13 tests in total.
        #
        # The grep is the point. Without it a failed library build would put every one of those
        # tests back to "skipped" and this job would go green again - reintroducing the exact
        # defect, in the exact place it was just removed from. Passing must require that they RAN.
        env:
          TRELYAN_REQUIRE_KAT: "1"
        run: |
          set -o pipefail
          pytest tests -v -rs 2>&1 | tee pytest-full.log
          if grep -q "FALCON_DET1024_LIB not" pytest-full.log; then
            echo "::error::lib-gated tests skipped in the one job that builds the library"
            exit 1
          fi

  contract-tests:
    name: Contract suite on LocalNet (real Falcon signatures, real AVM)
    # The contract suite has never run in CI. It is the only thing that exercises inscription.py
    # at all - falcon_verify on-chain, the C1 ownership conjunction, write-once, cross-cell replay,
    # the update_owner guards - and until now the only record that it passed was a hand-run note in
    # a markdown file dated 2026-06-01, two weeks BEFORE 2ec798e changed the contract.
    #
    # Running it needs three things, which is why it was never wired up:
    #   1. a LocalNet (algod on :4001)         - without it, 22x urllib.error.URLError
    #   2. the deterministic Falcon C library  - without it, 19x "Could not load Falcon shared
    #                                            library at './libfalcondet1024.so'"
    #   3. the committed typed client          - already in the repo (contracts/trelyan_client.py)
    # The pinned compile venv (puyapy) is NOT needed here: these tests deploy the committed client,
    # they do not rebuild the contract.
    runs-on: ubuntu-latest
    if: github.event_name != 'schedule'
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.3.1
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.12"   # algokit-utils / puya track <=3.13; 3.14 is not supported

      # Same pinned source + digest gate as signature-kat. These tests produce REAL det1024
      # signatures that the AVM's falcon_verify opcode must accept, so the signer has to be the
      # pinned build for the same reason that job says: a silent sampler or encoding change in a
      # deterministic signer is exactly the failure that must never pass unnoticed.
      - name: Stage the vendored pinned Falcon tree (commit ce15e75, no network)
        run: cp -r third_party/falcon-det1024/src falcon-src

      - name: Assert pinned-build digest + emulated FP backend
        run: python sdk/ci/verify_pinned_digest.py falcon-src

      - name: Build the deterministic Falcon-1024 shared library
        run: |
          cd falcon-src
          cc -O3 -fPIC -DFALCON_UNALIGNED=0 -fno-strict-aliasing -shared -o libfalcondet1024.so \
            codec.c common.c falcon.c fft.c fpr.c keygen.c rng.c shake.c sign.c vrfy.c deterministic.c
          echo "FALCON_DET1024_LIB=$PWD/libfalcondet1024.so" >> "$GITHUB_ENV"

      - name: Start LocalNet
        run: |
          python -m pip install --upgrade pip
          pip install algokit
          algokit localnet start
          # Do not proceed on a half-started sandbox: a suite that cannot reach algod fails with
          # connection errors that read like test failures, which wastes the signal this job exists
          # to give.
          for i in $(seq 1 30); do
            if curl -sf -m 5 http://localhost:4001/health \
                 -H "X-Algo-API-Token: $(printf 'a%.0s' $(seq 1 64))" -o /dev/null; then
              echo "algod healthy after ${i}s"; exit 0
            fi
            sleep 1
          done
          echo "::error::algod did not become healthy"; exit 1

      - name: Install the contract test dependencies
        run: pip install -r contracts/requirements.txt

      - name: Run the contract suite
        working-directory: contracts
        run: |
          set -o pipefail
          pytest test_inscription.py -v 2>&1 | tee contract-tests.log
          # Assert the suite actually EXERCISED the contract. pytest exits 5 on "no tests
          # collected", but a suite that silently shrank to a handful still exits 0, and this job
          # exists precisely because "green" has meant "nothing ran" here before.
          collected=$(grep -cE "^test_inscription\.py::.* (PASSED|FAILED)" contract-tests.log || true)
          echo "tests executed: $collected"
          if [ "$collected" -lt 20 ]; then
            echo "::error::only $collected contract tests executed; expected 20+"
            exit 1
          fi

  testnet-e2e:
    name: TestNet end-to-end (manual; needs DEPLOYER_MNEMONIC secret + generated client)
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.3.1
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.13"
      # This job SIGNS REAL TESTNET TRANSACTIONS, so its signer must be the pinned build - the same
      # one the KAT goldens were computed from. It previously used `git clone` of the default branch,
      # which is unpinned: upstream algorand/falcon resumed activity in June-July 2026, so that now
      # resolves to a DIFFERENT commit than the pin, and it skipped the digest gate entirely. For a
      # deterministic signer a silent sampler/encoding change is exactly the failure that must never
      # go unnoticed. Since 2026-08-18 the tree comes from third_party/ (vendored, byte-identical to
      # the pinned tarball) and the digest + FPEMU gate still runs before building, matching the
      # signature-kat job above.
      - name: Stage the vendored pinned Falcon tree (commit ce15e75, no network)
        run: cp -r third_party/falcon-det1024/src falcon-src

      - name: Assert pinned-build digest + emulated FP backend (fail fast if the tree drifted)
        run: python sdk/ci/verify_pinned_digest.py falcon-src

      - name: Build the deterministic Falcon-1024 shared library
        run: |
          cd falcon-src
          cc -O3 -fPIC -DFALCON_UNALIGNED=0 -fno-strict-aliasing -shared -o libfalcondet1024.so \
            codec.c common.c falcon.c fft.c fpr.c keygen.c rng.c shake.c sign.c vrfy.c deterministic.c
          echo "FALCON_DET1024_LIB=$PWD/libfalcondet1024.so" >> "$GITHUB_ENV"
      - name: Install SDK with the Algorand extra
        working-directory: sdk
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[algorand,dev]"
      - name: Reproducible TestNet inscribe (prints app + asset IDs to the run log)
        working-directory: sdk
        env:
          DEPLOYER_MNEMONIC: ${{ secrets.DEPLOYER_MNEMONIC }}
        run: |
          # Requires the generated typed client (trelyan_client.py) importable on PYTHONPATH,
          # produced from the contract ARC-56:  algokit generate client ... --output trelyan_client.py
          # Copy it next to examples/ or add contracts/ to PYTHONPATH, then:
          PYTHONPATH="src:../contracts:." python examples/quickstart.py
